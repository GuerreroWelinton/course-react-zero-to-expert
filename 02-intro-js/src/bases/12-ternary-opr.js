const active = true;

// let message = '';

// if (active === true) {
//   message = 'Active';
// } else {
//   message = 'Inactive';
// }

// const message = active === true ? 'Active' : 'Inactive';
// const message = active === true ? 'Active' : null;
const message = active === true && 'Active';
console.log(message);
